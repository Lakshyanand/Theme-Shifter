import React from "react";

const Header = () => {
    return(
        <header>
            <nav>
                Theme Switching App
            </nav>
        </header>
    );
};

export default Header;